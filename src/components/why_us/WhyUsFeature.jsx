import React from "react";

const WhyUsFeature = ({ title, text }) => {
  return (
    <div>
      <h4 className="font-sans">{title}</h4>
      <p>{text}</p>
    </div>
  );
};

export default WhyUsFeature;
